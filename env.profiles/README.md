# environment specific .profiles

Place these `env` and `env.*` in `.local/bin` for `base.profile` to load them automatically<br>
**note:** `env` loads first followed by all `env.*` in alphabetical order<br>
(use a naming convention to force order, eg. `env.01.*`, `env.02.*`)
